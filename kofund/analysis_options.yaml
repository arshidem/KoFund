# analysis_options.yaml - Basic version
analyzer:
  exclude:
    - '**/*.g.dart'
    - '**/*.freezed.dart'
    - '**/generated/**'
    - 'build/**'
  
  errors:
    # Treat these as errors (must fix)
    deprecated_member_use: error
    
    # Treat these as warnings (should fix)
    unused_import: warning
    unused_local_variable: warning
    
    # Downgrade these to info (can ignore)
    avoid_print: info
    prefer_final_fields: info
    use_build_context_synchronously: info
    unused_element: info
    unused_field: info
    unreachable_switch_case: info
    dead_code: info
    unnecessary_type_check: info

linter:
  rules:
    # Essential rules only
    - use_build_context_synchronously
    - avoid_print
    - prefer_final_fields
    - unused_import
    - unused_local_variable